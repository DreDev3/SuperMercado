import styled from 'styled-components';

export const Main = styled.main`
    position: relative;
    width: 90%;
    margin: auto;
    border-radius: 10px;
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 40px;

`;

